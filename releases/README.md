# ACSAD APK Releases

APK builds are generated by the **ACSAD Android APK** GitHub Actions workflow.

The APK is published as the `ACSAD-Android-1.1` workflow artifact after a successful build.

GitHub's repository file API used by this integration supports text files, not binary APK uploads, so the APK itself is not committed to this folder.
